class Settings():
    METHOD_NAME_TO_RUN = "run"
    # US Letter: 8.5 by 11 inches
    PAPER_LONG_SIDE_MM = 279.4
    PAPER_SHORT_SIDE_MM = 215.9
    # determines the size of the image produced,
    # independent of the source image resolution and the size of the paper in it
    PIXELS_PER_MM = 6
    STROKE_WIDTH_MM = 0.2
    DEFAULT_CAM_NO = 0
    SIMPLIFY_PATH_RADIUS_FACTOR_OF_STROKE_WIDTH = 6